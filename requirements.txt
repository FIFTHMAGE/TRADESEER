# Core Flask and web framework
Flask==2.3.3
requests==2.31.0

# Environment variables
python-dotenv==1.0.0

# Web3 and blockchain dependencies
web3==7.13.0
eth-account==0.13.7
eth-utils==5.3.0

# Cryptography for wallet encryption
cryptography==41.0.7

# Additional utilities
# Note: secrets, hashlib, sqlite3, threading, time, re, json, datetime, logging are built-in Python modules
# and don't need to be installed via pip 